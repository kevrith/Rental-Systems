VITE_API_URL=http://localhost:8000/api/v1

# OAuth client ID from https://console.cloud.google.com/apis/credentials — not
# a secret, it's compiled into this bundle (matches the backend's
# GOOGLE_CLIENT_ID). Leave blank to hide the "Sign in with Google" button.
VITE_GOOGLE_CLIENT_ID=
